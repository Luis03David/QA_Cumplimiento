const { test } = require('@playwright/test');

test('CP-01 borrado/cancelacion de datos: flujo visible y no destructivo por defecto', async ({ page }) => {
  test.skip(true, 'Faltante: el mecanismo de borrado/cancelacion de datos personales todavia no esta implementado en este nivel.');
});
